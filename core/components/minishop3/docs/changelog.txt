# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.6.0-beta1]

### Added
- Order cancel by customer in personal account: API endpoint, cancel button in order list/details, `ms3_customer_cancel_allowed_statuses` setting (#119, Issue #117)
- Remember active Vue tab across product page reloads via `localStorage`, `ms3_product_remember_tabs` setting (#120, Issue #111)

### Refactored
- Standalone LK modules (order-cancel, customer-addresses, auth-forms) integrated into ms3 architecture: API → UI class → hooks → message (#126)
- New `AuthUI` class for login/register forms with `beforeLogin`/`afterLogin`, `beforeRegister`/`afterRegister` hooks (#126)
- `CustomerUI` extended with order cancel, address management (set default, delete) with hooks (#126)
- `CustomerAPI` extended with `login`, `register`, `setDefaultAddress`, `cancelOrder` methods (#126)
- New `confirm.js` module: Promise-based Bootstrap Modal confirm dialog with native fallback, i18n by `<html lang>`, declarative binding via `data-ms3-confirm` attribute (#126)
- Order detail URLs now use UUID instead of integer ID for security (#126)
- UUID format validation for `$_GET['order']` parameter (#126)
- 6 new selectors in `Selectors.js` for LK components (#126)

### Fixed
- httpOnly cookie token architecture: single `ms3_token` httpOnly cookie as source of truth instead of 4 unsynchronized storages. Middleware injection for backward compatibility. Cart preserved on login/register (#124)
- Save button display for products and categories in MODX 3.2 — explicit `canSave`/`locked` calculation (#118)
- Access policy `data` format for MODX upgrade compatibility — fixed double JSON encoding (#107, Issue #100)

### Breaking Changes
- Register.php response: `token` field changed from object `{token, expires_at}` to plain string. `expires_at` moved to top level. Custom themes accessing `result.object.token.token` need update (#124)

## [1.5.0-beta1]

### Added
- Centralized selectors module (`Selectors.js`) with override via `ms3Config.selectors` (Issue #18)
- `ServiceCheckMiddleware` for graceful 503 on missing ms3 service (Issue #68)
- `has('ms3')` checks in all entry points, plugin events, and snippets
- `data-ms3-*` attributes as primary selectors, CSS classes as fallback (Issue #17)
- Rows per page selector, first/last pagination buttons, chunk hashes (Issue #78)
- `ms3:cart:updated` event on successful order submission (#96)
- `formatPrices` parameter for `msOrderTotal` snippet
- Sorting in orders list grid (Vue)

### Fixed
- Lexicon corrections (Issue #21)
- Removed `action` from miniShop3 menu configuration (#94)
- EAV options clearing from product form
- Empty orders list caused by unnecessary `GROUP BY`
- Idempotent grid seed migrations
- `Response::error` now includes correct HTTP status code in JSON response
- `CartController::change()`/`remove()` return type (array instead of Response object)
- Correct default order status IDs with fallback for zero values
- `getIterator` for msProduct/msCategory — added `class_key` to criteria

### Changed
- Removed redundant permission checks from processor `initialize()` methods (#95)
- Simplified `CustomerAddressController::getAuthorizedCustomer()` (middleware guarantees service)
- Removed fallback service registration in `api.php` — returns 503 on bootstrap failure

## [1.4.1-beta1] - 2026-02-14

### Added
- Tooltip with placeholder key on product fields
- HTML array format support for cart options
- QuantityUI module extracted from cart logic
- Gallery interface localization
- GitHub Actions workflow for automatic release creation

### Changed
- Minimum PHP version raised to 8.2
- All px units converted to rem in Vue components
- Dropdown components replaced with Select (PrimeVue 4)
- Unified placeholder/value display in form controls
- Text color variables updated for UI consistency

### Fixed
- Product copy no longer duplicates gallery images
- JSON fields (color, size) display in product template
- Vendor placeholder prefix (`vendor.` → `vendor_`)
- Snippet descriptions loading
- Calendar icon display unified across components

## [1.4.0-beta1] - 2026-02-07

### Added
- Notification Center: Email notifications on order status change (manager + customer recipients)
- Notification Center: Telegram notifications for managers on order status change
- Telegram settings: `ms3_telegram_bot_token`, `ms3_telegram_manager_chat_id`
- msOrder snippet: `customerFields` parameter for msCustomer field mapping
- msOrder snippet: Data source selection based on `ms3_customer_sync_enabled` setting
- msProducts snippet: `msOnProductsLoad` and `msOnProductPrepare` events for external packages
- msProducts snippet: `usePackages` parameter for bulk data loading (ms3Variants, msBrands)
- Help page: Migrated to Vue 3 + PrimeVue
- Resource to product conversion: Automatic msProductData creation on class_key change

### Changed
- All 41 services centralized in ServiceRegistry
- bootstrap.php now contains only core `ms3` service

### Fixed
- msOrder snippet: Removed dead POST validation code (validation in OrderSubmitHandler)
- msOrder template: Removed unused error CSS classes
- Vue components: Fixed `ms3.config` access (added `getMs3Config()` utility)
- XML schema and PHP models synchronization (stock, default_value, msGridField)
- Options tab styling fixes

### Removed
- Plupload library (gallery is Vue-based)
- Unused ExtJS files (product.grid.js, gallery/panel.js, model-fields.wrapper.js)
- Plupload lexicons
- Unused notification processors

## [1.3.0-beta1] - 2026-01-30

### Added
- Nested product tabs: Vue TabView inside ExtJS (Properties, Gallery, Categories, Links, Options)
- Plugin Registry API for third-party tabs: `window.MS3ProductTabsRegistry.register()`
- New extra field type "Dropdown" (xtype: `ms3-combo-select`) with static options
- Dynamic msOrderTotal widget update when cart changes
- New chunk `tpl.msOrderTotal` with default markup
- msProducts snippet: msCategoryMember support for additional categories
- msProducts snippet: own parents processing as workaround for pdoTools
- Tooltips for delivery/payment name fields (lexicon key support)
- VueTools added as automatic dependency

### Fixed
- Unified authentication in CustomerAddressController (API token + session customer_id)
- Migration for `select_options` column in `ms3_extra_fields` table

## [1.2.3-beta1] - 2026-01-24

### Added
- Automatic order cost recalculation when changing delivery or payment method
- Automatic delivery cost recalculation when cart changes (free delivery threshold support)
- Pagination for Vendors settings tab (previously limited to 20 records)
- Pagination for Product Links settings tab
- "Show nested" checkbox enabled by default in category products grid

### Fixed
- Order total cost now correctly includes delivery cost (`cost = cart_cost + delivery_cost`)
- Free delivery threshold calculation now uses cart cost passed to method
- Session token preserved during customer auto-registration (cart no longer disappears after page reload)
- Automatic token refresh on `ms3_err_token_invalid` error
- Order validation (delivery, payment) now runs before customer creation
- Payment method name translation in delivery edit dialog
- Validation rules dropdown z-index styling
- Form spacing in delivery and payment edit dialogs

## [1.2.2-beta1] - 2026-01-20

### Fixed
- Gallery image upload: fixed processor namespace escaping in JavaScript
- Clean installation: migrations now check table existence before creation
- Clean installation: all seed migrations use `sort_order` field (xPDO model compatibility)

## [1.2.1-beta1] - 2026-01-20

### Fixed
- MySQL 8.0 compatibility: renamed `rank` field to `sort_order` in `ms3_model_fields` and `ms3_model_field_sections` tables
- MySQL 8.0 compatibility: added backtick escaping for `rank` field in `modContext` system table
- Email verification letters not being sent (missing `modMail` import)
- Placeholder format in email lexicons (`{name}` → `[[+name]]`)
- Missing `minishop3:cart` lexicon loading in snippets

### Changed
- Orders grid: status now uses relation fields instead of direct JOIN
- Orders grid: added `order_status` (badge), `status_color`, `status_name` (relation) fields
- Orders grid: `delivery_name` and `payment_name` fields now use `relation` type

## [1.2.0-beta1] - 2026-01-16

### Added
- Order validation for delivery required fields (from `validation_rules`)
- Customer data validation (email/phone) when creating from order
- Customer creation during order finalization from address data
- Duplicate customer handling during order finalization
- Validation error lexicons for order fields (ru/en)
- OrderFinalizeService for order finalization logic

### Changed
- "Create Order" button renamed to "Create Draft Order"
- Custom routes can now override system routes
- Modernized thumbnail generation API
- Improved product selection UI in order management
- Moved inline styles to centralized CSS file

### Fixed
- Automatic token renewal instead of error on expiration
- Delivery required fields validation during order finalization
- Customer creation flow in admin order management
- Gallery upload/delete 500 error (incorrect getObject/getIterator usage, rank → position field)

## [1.1.0-beta1] - 2026-01-11

### Added
- ProductCardUI module for adaptive cart buttons in product cards
- Cart state display: "Add to cart" button or quantity controls (+/-)
- Automatic UI update when cart changes via `ms3:cart:updated` event
- New lexicons: `ms3_cart_add`, `ms3_cart_in_cart`
- Migration for adding ProductCardUI.js to frontend assets
- Tab descriptions for Utilities page tabs explaining each section purpose
- CSS class `.tab-description` for consistent tab description styling

### Changed
- Product card template (`ms3_products_row.tpl`) with dual-state cart forms
- Product page template (`product.tpl`) with cart state awareness
- Base template with `icon-plus` and `icon-minus` SVG icons
- Renamed "Расширение объектов" tab to "Свои поля" (Custom Fields)
- Renamed "Управление свойствами товара" to "Поля товара (админка)"
- Renamed "Конфигурация полей моделей" to "Поля форм (админка)"
- Improved layout: dropdown and button on same row in "Свои поля" and "Поля таблиц" tabs
- Removed duplicate titles from tab content (already shown in tab headers)

### Fixed
- CSS isolation for Dialog and ConfirmDialog components (appendTo="self")
- Product options (color, size) saving in msProductOption table
- Duplicate Delivery/Payment filters on Orders page
- Field visibility saving in product properties management
- Migration errors on clean installation
- Default value error for `created_at` field

## [1.0.0-beta1] - 2026-01-05

First beta release of MiniShop3 for MODX 3.

### Added
- Modern admin interface with Vue 3 + PrimeVue components
- Service-based architecture with dependency injection
- Flexible field configuration system for products, orders, customers
- Grid configuration system with customizable columns and actions
- API router with FastRoute for REST endpoints
- Customer management with addresses and order history
- Order management with status workflow and notifications
- Delivery and payment methods configuration
- Product gallery with drag-and-drop upload
- Extra fields system for extending entities
- Import functionality for products
- Notification system for order events
- Full lexicon support (Russian, English)

### Changed
- Complete rewrite for MODX 3.0+ compatibility
- PHP 8.1+ required
- Namespace changed to MiniShop3
- Database tables prefixed with ms3_
- ExtJS panels integrated with Vue components

### Dependencies
- Requires VueTools package for Vue infrastructure
- Uses PdoTools for template rendering

## [1.0.0-alpha] - 2025

- Initial alpha releases for testing and development.
